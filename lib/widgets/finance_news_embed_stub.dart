import 'package:flutter/widgets.dart';

/// Stub: mobile usa [WebViewWidget]; não usar este widget fora da Web.
Widget buildFinanceNewsEmbed(String url, {Key? key}) {
  return SizedBox.shrink(key: key);
}
