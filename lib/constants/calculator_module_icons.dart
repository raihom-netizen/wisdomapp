import 'package:flutter/material.dart';

/// Ícones do módulo Calculadora Banco de Horas (navegação + faixa premium).
abstract final class CalculatorModuleIcons {
  /// Rail, bottom nav e item no menu lateral (ícone de calculadora, não Σ/funções).
  static const IconData nav = Icons.calculate_rounded;

  /// Cabeçalho premium da tela.
  static const IconData banner = Icons.auto_awesome_rounded;
}
